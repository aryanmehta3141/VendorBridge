export async function createPurchaseOrderRecord(_data: unknown): Promise<{ message: string }> {
  return { message: "PO service - coming soon" };
}

export async function getAllPurchaseOrders(): Promise<never[]> {
  return [];
}
